package com.leecoder.network.const

import com.leecoder.stockchart.network.BuildConfig

object Credential {
    const val CLIENT_CREDENTIAL = "client_credentials"
    const val APP_KEY = BuildConfig.AppKey
    const val APP_SECRET = BuildConfig.AppSecret
}